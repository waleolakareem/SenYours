// $("#calendar_ajax_remove").remove()
// $("<%=j render :partial => 'calendar' %>").appendTo('#calendar_ajax_append')

// Render anything that has to do with acceptance:
// Such as:
// Calendar - Transactions - Comp_show(top part)
// Needed variables:
// date - senior_id - companion_id - accept is assumed as its the accepted route -

// @date, @companion_id, & @senior_id is passed in from 'accept_appointment' route in

// Calendar Selector: cancel_appointment_#{appointment.id}
// Calendar Selector: set_date_remove_#{date}
// Calendar Selector: set_date_add_#{date}



$('#this_is_bob').html('Billy!')
// $("#calendar_ajax_append").html("<%=j render :partial => 'calendar' %>")
